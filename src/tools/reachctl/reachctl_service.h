#ifndef REACHCTL_ELEVATION_HELPER_H
#define REACHCTL_ELEVATION_HELPER_H

#include "reach/support/util.h"

reach_result reachctl_install_service(const wchar_t *user_id);
reach_result reachctl_unregister_service(void);

#endif
